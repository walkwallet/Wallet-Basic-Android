<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/basic_dialog_background"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingStart="@dimen/basic_dialog_horizontal_padding"
    android:paddingTop="@dimen/basic_dialog_vertical_padding"
    android:paddingEnd="@dimen/basic_dialog_horizontal_padding"
    android:paddingBottom="@dimen/basic_dialog_vertical_padding">

    <ImageView
        android:id="@+id/iv_icon"
        android:layout_width="@dimen/basic_dialog_icon_width"
        android:layout_height="@dimen/basic_dialog_icon_height"
        android:layout_marginBottom="@dimen/basic_dialog_icon_bottom_spacing"
        android:visibility="gone" />

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginBottom="@dimen/basic_dialog_title_bottom_spacing"
        android:textColor="?attr/basic_dialog_title_text_color"
        android:textSize="@dimen/basic_dialog_title_text_size" />

    <FrameLayout
        android:id="@+id/fl_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/basic_dialog_content_bottom_spacing"
        android:visibility="gone">

        <TextView
            android:id="@+id/tv_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:textColor="?attr/basic_dialog_content_text_color"
            android:textSize="@dimen/basic_dialog_content_text_size" />

    </FrameLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/bt_negative"
            style="?attr/basic_dialog_negative_button_style"
            android:layout_width="0dp"
            android:layout_height="@dimen/basic_dialog_button_height"
            android:layout_weight="1"
            android:textSize="@dimen/basic_dialog_button_text_size"
            android:visibility="gone" />

        <Button
            android:id="@+id/bt_positive"
            style="?attr/basic_dialog_positive_button_style"
            android:layout_width="0dp"
            android:layout_height="@dimen/basic_dialog_button_height"
            android:layout_marginStart="@dimen/basic_dialog_button_spacing"
            android:layout_weight="1"
            android:textSize="@dimen/basic_dialog_button_text_size"
            android:visibility="gone" />
    </LinearLayout>
</LinearLayout>
